import React from "react";

const cars = ["bmw", "lambo", "honda", "swift"];
const bike = ["ninja", "pulsar", "h2r", "platina"];
const Both = [...cars, ...bike];
console.log(Both);

export default Both;
